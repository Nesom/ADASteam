@*@model BasketModel

    @foreach (var element in Model.Products)
    {
        <form>
            <pre style="font-size:15px">@element.Key.Name --- Цена: @element.Key.Price --- Количество: @element.Value --- <a asp-action="RemoveProduct/@element.Key.Id">Удалить из корзины</a> --- Количество: <input type="number" max="@element.Key.Count" min="1" name="count" /><input type="number" value="@element.Key.Id" name="Id" hidden /><input type="submit" asp-action="ChangeCount" formmethod="post" name="Изменить" /></pre>
            <pre><a asp-controller="Product" asp-action="Get/@element.Key.Id">Посмотреть подробную информацию</a></pre>
        </form>
    }
    @if (Model.Products.Count != 0)
    {
        <pre>Сумма заказа: @Model.Amount руб</pre>
        <pre><a asp-controller="Order" asp-action="Sending">Заказать</a></pre>
    }*@

<!DOCTYPE HTML>
<html>
<head>
    <title>Internet-shop ADAS</title>
    <link href="css/bootstrap.css" rel='stylesheet' type='text/css' />
    <!-- jQuery (necessary JavaScript plugins) -->
    <script type='text/javascript' src="js/jquery-1.11.1.min.js"></script>
    <!-- Custom Theme files -->
    <link href="css/style.css" rel='stylesheet' type='text/css' />
    <!-- Custom Theme files -->
    <!--//theme-style-->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="keywords" content="Gretong Responsive web template, Bootstrap Web Templates, Flat Web Templates, Andriod Compatible web template,
Smartphone Compatible web template, free webdesigns for Nokia, Samsung, LG, SonyErricsson, Motorola web design" />
    <script type="application/x-javascript"> addEventListener("load", function() { setTimeout(hideURLbar, 0); }, false); function hideURLbar(){ window.scrollTo(0,1); } </script>
    <link href='http://fonts.googleapis.com/css?family=Roboto:400,100,300,500,700,900' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Playfair+Display:400,700,900' rel='stylesheet' type='text/css'>
    <!-- start menu -->
    <link href="css/megamenu.css" rel="stylesheet" type="text/css" media="all" />
    <script type="text/javascript" src="js/megamenu.js"></script>
    <script>$(document).ready(function () { $(".megamenu").megamenu(); });</script>
    <script src="js/menu_jquery.js"></script>
    <script src="js/simpleCart.min.js"></script>
</head>
<body>
    <!-- header_top -->
    <!-- header -->
    <div class="header_bg">
        <div class="container">
            <div class="header">
                <ul class="megamenu skyblue">
                    <li class="active grid"><a class="color1" asp-controller="Home" asp-action="Index">Home</a></li>
                    <li class="active grid"><a class="color1" asp-controller="Home" asp-action="Catalog">Catalog</a></li>
                </ul>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="check">
            <div class="col-md-3 cart-total">
                @*<a class="continue" href="#">Continue to basket</a>*@
                <div class="price-details">
                    <h3>Price Details</h3>
                    <span>Total</span>
                    <span class="total1">@Model.Amount</span>
                    <span>Discount</span>
                    <span class="total1"></span>
                    <div class="clearfix"></div>
                </div>
                <ul class="total_price">
                    <li class="last_price"> <h4>TOTAL</h4></li>
                    @if (Model.Products.Count != 0)
                    {
                        <li style="margin-top:10px"class="last_price"><span>@Model.Amount</span></li>
                        <h4><a asp-controller="Order" asp-action="Sending">Order</a></h4>
                    }
                </ul>

                

                <div class="clearfix"></div>
                @*<a class="order" href="#">Place Order</a>
                    <div class="total-item">
                        <h3>OPTIONS</h3>
                        <h4>COUPONS</h4>
                        <a class="cpns" href="#">Apply Coupons</a>
                        <p><a href="#">Log In</a> to use accounts - linked coupons</p>
                    </div>*@
            </div>
            <div class="col-md-9 cart-items">
                <h1>My Shopping Bag (@Model.Products.Count)</h1>
                @foreach (var element in Model.Products)
                {
                    <form>
                        <pre style="font-size:15px">@element.Key.Name   Price: @element.Key.Price   Count: @element.Value --- <a asp-action="RemoveProduct/@element.Key.Id">Remove</a>  Set count: <input type="number" max="@element.Key.Count" min="1" name="count" /><input type="number" value="@element.Key.Id" name="Id" hidden /><input type="submit" asp-action="ChangeCount" formmethod="post" name="Submit" />     <a asp-controller="Product" asp-action="Get/@element.Key.Id">Detalis...</a></pre>
                    </form>
                }
                @if (Model.Products.Count != 0)
                {
                    <h3 style="margin-top:10px">Amount: @Model.Amount руб</h3>
                    <h4 style="margin-top:10px"><a asp-controller="Order" asp-action="Sending">Order</a></h4>
                }
            </div>
            <div class="clearfix"> </div>

        </div>
    </div>
</body>
</html>

